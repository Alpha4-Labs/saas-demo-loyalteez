/** @type {import('next').NextConfig} */
const nextConfig = {
  // Disable standalone output - Cloudflare Pages handles this
  // output: 'standalone', // REMOVED - causes issues with Cloudflare Pages
};

module.exports = nextConfig;
